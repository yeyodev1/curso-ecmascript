let hello = "Hello";
let world = "world";

let epicPhrase = hello + " " + world + "!";

// template literals
let epcPhrase2 = `${hello} ${world}!`;
console.log(epcPhrase2);

// Multi-line strings
let lorem = "Esto es un string \n" + "esto es otra linea";

let lorem2 = `Esta es un frase epica
   la continuacion de esa fase epica
`;
console.log(lorem);
console.log(lorem2);
